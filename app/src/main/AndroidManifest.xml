<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.myexpenses">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.expense_list_report.ExpenseListReportActivity">
        </activity>
        <activity android:name=".activity.expense_details.ExpenseDetailsActivity">
        </activity>
        <activity android:name=".activity.save_expense.SaveExpenseActivity">
        </activity>
        <activity
            android:name=".activity.log_in.LogInActivity"
            android:label="@string/title_activity_log_in"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.create_spender.CreateSpenderActivity">
        </activity>
        <activity android:name=".activity.expense_list.ExpenseListActivity">
        </activity>
        <activity android:name=".activity.create_expense_list.CreateExpenseListActivity">
        </activity>
        <activity android:name=".activity.create_category.CreateCategoryActivity">
        </activity>
    </application>

</manifest>